import { component$, useContext } from "@builder.io/qwik";

import { Button } from "~/recipes/button";
import { inputStyle } from "~/recipes/input";
import { todoListCtxId, ItemProps } from ".";

export const AddItem = component$(() => {
    //const todoItems: ItemProps[] = useContext(todoListCtxId);

    return <></>;
});
